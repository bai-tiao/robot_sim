# =============================================================
# Nav2 参数配置文件 (占位)
# nav2_params.yaml
#
# 定位 : FAST-LIO2 (外部提供 TF: map -> odom -> base_link)
# 全局规划 : 待定 【TODO】确认算法后补充
# 局部规划 : 待定 【TODO】确认算法后补充 (可能为 ego_planner)
#
# 此文件目前仅作结构占位, 不可直接运行。
# 确认导航算法后再填写各 plugin 参数。
# =============================================================
